{
  "name": "padelparty-backend",
  "version": "1.0.0",
  "private": true,
  "description": "PadelParty backend — accounts + shared data (pure Node, zero deps).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
